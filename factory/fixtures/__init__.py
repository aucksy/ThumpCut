"""Fixture audio for running the Factory without credentials.

The three WAV files here are synthesised by ``make_fixtures.py``. They are original work with
no licence obligations, and each has an exact known tempo so the beat detector can be tested
against ground truth rather than against itself.
"""
