node_modules/
.env
.env.local
factory/tmp/
factory/out/
# The owner's own music, for hearing the cuts against a track he knows. Never committed —
# the README that explains the folder is, so the folder exists in a fresh clone.
factory/local/*
!factory/local/README.md
dist/
build/
*.log
.expo/
.expo-shared/
__pycache__/
.pytest_cache/
.venv/
artifacts/
*.tsbuildinfo

# The native projects Expo generates on every build. Anchored to where they are generated:
# an unanchored `android/` also matched app/modules/*/android, which is hand-written source,
# and quietly kept the entire renderer and the Instagram share module out of the repository.
/app/android/
/app/ios/
.DS_Store
Thumbs.db
