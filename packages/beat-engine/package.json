{
  "name": "@thumpcut/beat-engine",
  "version": "0.1.0",
  "private": true,
  "description": "The Factory's spectral_dp beat detector, ported to pure TypeScript so a phone can analyse the user's own music. Zero runtime dependencies.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types \"test/*.test.ts\""
  },
  "dependencies": {
    "@thumpcut/cut-engine": "*"
  },
  "sideEffects": false
}
