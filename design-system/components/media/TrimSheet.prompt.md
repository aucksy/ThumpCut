Half-height bottom sheet opened from a clip's duration badge. Clip loops on top, filmstrip scrubber below, teal in-point handle already nudged into the clip (teaches the feature quietly).

```jsx
<TrimSheet clipSrc={u} inPoint="0:02.4" duration="0:12" inPct={18} />
```

Exact copy: "Pick the moment this clip starts from." In-point reads `IN 0:02.4` in teal mono.