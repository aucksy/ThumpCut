Template gallery card: 9:16 preview, Archivo name, mono `128 BPM · 8–16 items`, compressed beat ruler along the bottom.

```jsx
<TemplateCard name="Night drive" bpm={128} items="8–16" src={u} />
<TemplateCard still src={u} />      // uncached: first frame as a still
<TemplateCard selected src={u} />   // preview strip selection
```